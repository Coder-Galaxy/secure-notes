# All environment variables for docker-compose
# Empty since we use Vault for secrets, but required by some tools if we want to add non-secret env vars
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=root
DISABLE_VAULT=false
